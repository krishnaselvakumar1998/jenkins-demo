pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Jenkins pulled code from GitHub successfully'
                sh 'ls'
            }
        }
    }
}

